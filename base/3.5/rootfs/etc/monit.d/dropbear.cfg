check process dropbear with pidfile /var/run/dropbear.pid
    start program = "/usr/sbin/dropbear -R -B -E -a"
    stop program = "/usr/bin/pkill dropbear"
    if failed port 22 protocol ssh then restart
    onreboot nostart
