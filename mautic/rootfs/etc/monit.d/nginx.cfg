check process nginx with pidfile "/var/run/nginx.pid"
    start program = "/bin/process_wrapper start nginx nginx -g 'daemon off; pid off;'"
    stop program = "/bin/process_wrapper stop nginx"
    onreboot nostart
